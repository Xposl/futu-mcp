export enum RetType {
  // 成功
  RetType_Succeed = 0,
  // 失败
  RetType_Failed = -1,
  // 超时
  RetType_TimeOut = -100,
  RetType_DisConnect = -200,
  // 未知结果
  RetType_Unknown = -400,
  RetType_Invalid = -500
}