export default class InitPromise {
    private isInit;
    private resolveFunc;
    private rejectFunc;
    reset(): void;
    get isReady(): Promise<void>;
    resolve(): Promise<void>;
    reject(_any: any): Promise<void>;
}
